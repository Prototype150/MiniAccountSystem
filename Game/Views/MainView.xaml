<UserControl x:Class="Game.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Game.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Button Height="100" Width="100" VerticalAlignment="Top" HorizontalAlignment="Left" Content="Log Out" FontSize="20" Command="{Binding LogOutCommand}"></Button>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding CurrentAccount.Username}" FontSize="60"/>
    </Grid>
</UserControl>
